{
  "goal_summary": "Implement PR 23 (RouterStream: mid-stream streaming fallback and observation) of the nygen-router package, in place in the existing repository, exactly as pinned in Projectplan/ProjectPlan.md's PR 23 section including its 2026-07-17 design-interview revision note. This is an implementation task, not a design task: every design decision is already made; if this spec and the codebase genuinely conflict, stop and report the conflict in the completion report instead of inventing a resolution.",
  "deliverable_type": "code module - a complete, tested, documented PR implemented in place in the ProviderRouterPR1/ Python package",
  "parameters": {
    "tone": "Match the existing codebase exactly: same naming, idiom, and comment density; comments state constraints the code cannot show, never narrate changes. README prose matches its existing plain, example-first register.",
    "length": "One scoped PR. Expected touch set: src/nygen_router/errors.py, adapters/base.py, adapters/openai_compatible.py, router.py, metrics.py, storage/base.py, storage/duckdb.py, storage/sqlite.py, __init__.py, README.md, AGENT.md, a new tests/test_router_stream.py, plus in-place updates to existing test files where behavior deliberately changed. No unrelated files.",
    "format": "In-place changes on branch main; git tag pr23-start before changes and pr23-complete when all quality gates pass; commits at logical milestones; final message ends with the five-section completion report defined in context_and_facts.",
    "audience": "The nygen-router maintainer, reviewing this as a scoped PR against the pinned plan."
  },
  "must_include": [
    "1. NormalizedStream ABC in adapters/base.py, stdlib-only imports: abstract __next__ (yields the SDK's raw chunks; any exception escaping it must already be a router error), abstract properties completed (a completion marker was seen) and usage (provider-reported usage object or None), abstract close(); __iter__ returns self. Exported from the package root so custom adapters can opt in.",
    "2. OpenAIChatStream(NormalizedStream) in adapters/openai_compatible.py: delegates next() to the underlying openai.Stream; yields chunks unchanged with no buffering; on exception re-raises the mapped router error chained via 'raise ... from'; sets completed when any choices-bearing chunk carries a finish_reason; tracks whether any recognized (choices-bearing) chunk was ever seen; pockets chunk.usage when present (the include_usage final chunk has empty choices and arrives after the finish_reason chunk - it must not disturb completed or recognized tracking); close() delegates to the SDK stream.",
    "3. Extract the adapter's existing except-chain into one shared module-level _map_sdk_exception helper used by both invoke() and OpenAIChatStream - one copy of the mapping. Add httpx transport branches (httpx.TimeoutException -> ProviderTimeoutError, httpx.TransportError -> ProviderConnectionError) because raw transport errors can escape unwrapped during SSE iteration. The dispatch-specific TypeError/AttributeError handling (InvalidOperationArgumentsError/UnsupportedOperationError) stays in invoke() only - a mid-stream TypeError must never map to InvalidOperationArgumentsError.",
    "4. adapter.invoke() wraps an openai.Stream result in OpenAIChatStream before returning (openai is already imported there); non-stream results are returned exactly as today.",
    "5. router.py stays 100% SDK-free (no openai or httpx import, not even lazy). Stream detection is isinstance(response, NormalizedStream). For streams, invoke() returns a RouterStream and defers ALL success recording - metrics AND health record_success - to stream end; the non-streaming path stays byte-identical to today's behavior.",
    "6. RouterStream (in router.py): pass-through iterator over the current NormalizedStream, carrying the not-yet-tried tail of the ranked provider order, the variants-by-protocol mapping, and the attempts list (including pre-stream failures). On a mid-stream router error or a truncation verdict: append the failed ProviderAttempt, categorize, record the failure metrics event, and unless the category is a STOP category apply health record_failure; STOP categories abort immediately under both policies; policy RAISE re-raises the provider's real error after recording; otherwise restart on the next provider (each eligible provider tried at most once per call); when no providers remain raise RouterExhaustedError enumerating every attempt's own real reason.",
    "7. stream_failure_policy: a StrEnum (values 'restart' and 'raise', default RESTART) as a ProviderRouter constructor argument. No per-call override.",
    "8. Truncation verdict only with evidence: clean end with recognized chunks and no finish_reason -> synthesize ProviderStreamInterruptedError and treat as a mid-stream failure; clean end where no chunk was ever recognized -> treat as completed and log DEBUG. The first unrecognized-shape stream per operation string per router instance logs one WARNING stating truncation detection is not available for that operation's stream shape; repeats log DEBUG (the PR 4/PR 5 dedup pattern).",
    "9. ProviderStreamInterruptedError: new ProviderError subclass in errors.py. New ErrorCategory.STREAM_INTERRUPTED member; categorize_error() gains its isinstance branch; it joins health.py's _COUNTED_CATEGORIES (counts toward failure_threshold) and is never in _STOP_CATEGORIES.",
    "10. Health timing mirrors metrics: record_success() fires only at completed stream end; a mid-stream failure records at failure time; stream open touches neither health nor metrics.",
    "11. Consumer close semantics: RouterStream.close() always closes the underlying stream, never triggers a restart, is idempotent, and is terminal - later iteration raises StopIteration (Python closed-generator semantics). At close: if completed was already seen, record the observed success (metrics + health); otherwise record nothing at all - the one documented exception to one-event-per-attempt. Context-manager support (__enter__/__exit__ calling close). A bare break without close runs no router code and records nothing.",
    "12. on_restart: ProviderRouter constructor argument, default None. Fires only when at least one chunk had already been yielded; receives one frozen dataclass (suggested name StreamRestart) with fields: failed provider name, the real error object, next provider name, chunks already yielded, restart count. A restart with chunks yielded and no callback registered logs a stdlib WARNING. A public restarts counter lives on RouterStream. The callback's own exceptions propagate - never swallowed.",
    "13. Restart anomaly (defensive): if a restart's adapter.invoke() returns something that is not a NormalizedStream, close it if possible, record the attempt as failed with a synthesized ProviderError naming the cause, and continue the fallback - a non-stream response can never be yielded to a consumer iterating chunks.",
    "14. Metrics schema: MetricsEvent gains stream: bool = False and total_duration_ms: float | None = None. provider_attempts gains stream INTEGER NOT NULL DEFAULT 0 and total_duration_ms REAL via the shared SQL in storage/base.py, identically in DuckDB and SQLite. On-init migration: after CREATE TABLE IF NOT EXISTS, check existing columns and ALTER TABLE ADD COLUMN any that are missing - this establishes the convention PR 24 and PR 6 reuse. event_to_params and row_to_event stay mirrored in column order.",
    "15. Stream-row timing semantics: latency_ms always means time-to-first-chunk (TTFT), NULL if no chunk ever arrived - never repurposed for another quantity; total_duration_ms records open-to-end on completion AND open-to-death on mid-stream failure, and the duration also appears in the failure's log line. A streaming call that fails before the stream opens is recorded stream=0 (no response exists to detect, so the flag means 'a stream actually opened'). A stream's metrics event is written at stream end by RouterStream, not at invoke() return.",
    "16. Usage: pocketed by the wrapper and exposed via NormalizedStream.usage only. Zero persistence of usage in this PR - no token columns, no MetricsEvent fields for tokens (they are PR 24's).",
    "17. Structure rule: the rule-carrying steps (metrics recording, health transitions, failure classification, per-provider argument building - extract the model-injection expression into a small shared helper) each exist exactly once as shared helpers; invoke() and RouterStream keep two thin separate loops; no attempt-engine rewrite of invoke().",
    "18. Exports from the package root: NormalizedStream, StreamFailurePolicy, the StreamRestart dataclass, ProviderStreamInterruptedError. The bare-import rule still holds.",
    "19. Tests: implement the PR 23 test list from ProjectPlan.md in full (as amended by the 2026-07-17 revision - including the unrecognized-shape, close-semantics, health-at-stream-end, and STREAM_INTERRUPTED-counting bullets), driving streams through fake NormalizedStream implementations and the existing constructor seams; openai-SDK-level wrapper tests drive real SDK streaming responses through the http_client seam with httpx.MockTransport SSE responses.",
    "20. Docs: README gains a streaming section (consumer code is unchanged versus the raw SDK; restart-means-regeneration and the discard rule; on_restart example; close/context-manager recommendation for early stops; stream_failure_policy; what the unrecognized-shape warning means; custom adapters opt in by returning a NormalizedStream). AGENT.md updated wherever it describes the feature set, constructor parameters, and error taxonomy.",
    "21. Quality gates all pass in the project's own .venv: ruff format ., ruff check ., mypy src, pytest, coverage run -m pytest, coverage report; plus the non-negotiable check that 'from nygen_router import ProviderRouter' succeeds with no optional dependencies installed."
  ],
  "must_avoid": [
    "Adding input_tokens/output_tokens columns or persisting usage anywhere (PR 24 owns that).",
    "Redesigning, expanding, or second-guessing any pinned decision; no incidental improvements or drive-by refactors beyond the named helper extractions.",
    "monkeypatch.setattr / unittest.mock.patch of internal collaborators, and any test that sleeps; monkeypatch.setenv/delenv for environment variables remain allowed.",
    "Deleting existing tests; update in place only where this PR deliberately changes asserted behavior.",
    "Any openai or httpx import in router.py (even lazy), and any module-level SDK import anywhere.",
    "Buffering, altering, reordering, or synthesizing chunks; no router-invented marker objects in the stream.",
    "Per-call overrides for stream_failure_policy or on_restart.",
    "Async/await support - the codebase is synchronous.",
    "Retry-After parsing, max-restart knobs, escalating cooldowns, give-up-forever counters (deferred or rejected backlog).",
    "Changing PR 5's health transition rules beyond adding STREAM_INTERRUPTED to the counted categories.",
    "Recording exclusions, changing non-streaming recording semantics, or adding new runtime dependencies."
  ],
  "steps": [
    { "step": 1, "instruction": "Read in full: Projectplan/ProjectPlan.md (Project goal, Core design principles, Testing philosophy, the transparency principle in PR 1's key decisions, and the PR 23 section INCLUDING the 2026-07-17 design-interview revision note - that note is the authoritative decision record this spec compresses), then the current source under ProviderRouterPR1/src/nygen_router/ and tests under ProviderRouterPR1/tests/." },
    { "step": 2, "instruction": "Git preflight: confirm the working tree is clean (stop and report if not) and ensure tag pr23-start exists on the current commit." },
    { "step": 3, "instruction": "errors.py: add ProviderStreamInterruptedError and ErrorCategory.STREAM_INTERRUPTED with its categorize_error branch (must_include 9)." },
    { "step": 4, "instruction": "adapters/base.py: add the NormalizedStream ABC (must_include 1)." },
    { "step": 5, "instruction": "adapters/openai_compatible.py: extract _map_sdk_exception with the new httpx branches, add OpenAIChatStream, and wrap stream results in invoke() (must_include 2-4). Verify empirically which exception types the openai SDK actually raises during stream iteration - the plan flags this medium-confidence - and make the shared helper cover what you observe plus the documented families. Remember the SDK returns openai.Stream when response headers arrive, before any tokens." },
    { "step": 6, "instruction": "health.py: add STREAM_INTERRUPTED to _COUNTED_CATEGORIES (must_include 9); no other transition-rule changes." },
    { "step": 7, "instruction": "router.py: add StreamFailurePolicy, the StreamRestart dataclass, the on_restart and stream_failure_policy constructor parameters, the isinstance(NormalizedStream) fork in invoke() with all stream success recording deferred, the shared argument-building helper, and RouterStream implementing must_include 6-13 and 15." },
    { "step": 8, "instruction": "metrics.py and storage/{base,duckdb,sqlite}.py: add the stream and total_duration_ms fields/columns and the on-init check-and-ALTER migration convention (must_include 14)." },
    { "step": 9, "instruction": "__init__.py: add the new exports (must_include 18)." },
    { "step": 10, "instruction": "Tests: new tests/test_router_stream.py plus in-place updates, covering the full amended PR 23 test list under the binding testing rules (must_include 19); protocol-conformance tests in test_metrics_store.py get the new columns' round-trip." },
    { "step": 11, "instruction": "Docs: README streaming section and AGENT.md updates (must_include 20)." },
    { "step": 12, "instruction": "Run all quality gates in .venv and the bare-import check (must_include 21); tag pr23-complete on the final commit; end with the five-section completion report from context_and_facts." }
  ],
  "context_and_facts": [
    "Repository layout: the package lives in ProviderRouterPR1/ (pyproject.toml, src/nygen_router/, tests/); the project venv is ProviderRouterPR1/.venv; the plan is Projectplan/ProjectPlan.md. Work on branch main, in place.",
    "Current behavior being changed: invoke() (router.py fallback loop) treats adapter.invoke() returning as success and records metrics + health at that moment; for a streaming call that is merely 'headers arrived'. The adapter maps SDK exceptions only around the .create() call; exceptions during stream iteration currently escape as raw openai/httpx types. No streaming-specific code exists anywhere in src/ or tests/ (verified by grep).",
    "Post-PR-3R contract: invoke(calls: list[CallVariant]) returns the winning provider's raw SDK response object (Any); there is no response wrapper; attempts/exclusions surface only through RouterExhaustedError.attempts and NoEligibleProvidersError.exclusions. The router injects the provider's configured model into a copy of the variant's arguments per attempt and never mutates the CallVariant.",
    "PR 5 machinery to build on, not modify: ProviderHealthState.record_failure(category, error_text, config, now) -> bool (True = new bench, triggering _log_bench) and record_success(); benched/warned flags drive bench-episode log dedup; _COUNTED_CATEGORIES lives in health.py; _STOP_CATEGORIES (BAD_REQUEST, INVALID_OPERATION) lives in router.py; cooldowns run on the injected clock seam (clock: Callable[[], float] = time.monotonic).",
    "Existing constructor seams for tests (no monkeypatching of internals is allowed): adapter_factory, policy, supported_protocols, metrics_store (None disables; _UNSET sentinel defaults to DuckDBMetricsStore), health, clock; the openai adapter takes http_client for httpx.MockTransport injection.",
    "storage/base.py holds the shared SQL (CREATE/INSERT/SELECT constants) plus event_to_params/row_to_event used by both DuckDBMetricsStore and SQLiteMetricsStore so the engines stay byte-identical; keep column order mirrored across all of them when adding columns. Timestamps are ISO-8601 UTC text generated in Python.",
    "openai SDK facts: with stream=True the SDK returns an openai.Stream as soon as response headers arrive; TTFT is observable only at first-chunk arrival inside iteration, so RouterStream must receive the attempt start time from invoke() and timestamp the first chunk itself. With stream_options include_usage, the final chunk carries usage, has an empty choices list, and arrives after the finish_reason chunk.",
    "Transparency principle (binding): never wrap, rephrase, or blend a provider's own error; chain originals via 'raise ... from'; total-failure errors enumerate each provider's real, distinct reason.",
    "Testing philosophy (binding): no monkeypatching of internal collaborators; production code exposes constructor seams instead; tests are additive - never deleted, updated in place only for deliberately changed behavior.",
    "Non-negotiable import rule: 'from nygen_router import ProviderRouter' must work with no optional dependencies installed; provider SDKs are lazy-imported inside adapter methods only.",
    "Completion report format (required, verbatim section names, in this order): '## Files changed' (one bullet per file); '## Design decisions checklist' (one line per must_include item 1-21, each ending IMPLEMENTED AS SPECIFIED or 'DEVIATED: <one-line reason>'); '## Behavior changes' (what a library user observes differently); '## Quality gates' (the six commands with verbatim output, pytest trimmed to summary, plus the bare-import check result); '## Deviations and concerns' (interpretations, conflicts, untestable items - or the single word 'None').",
    "Where this JSON compresses detail, the PR 23 section of Projectplan/ProjectPlan.md including the 2026-07-17 revision note is authoritative and wins."
  ],
  "assumptions_made": [
    "This file (Projectplan/prompt_pr23.txt) is itself the planning-stage output: pure JSON handed to the executing model, deliberately departing from the prose format of prompt_pr2-pr5 because the pipeline template mandates this structure and output-only format.",
    "The template's parameters fields (tone/length/format/audience) were written for content deliverables; they are mapped onto a code deliverable as code style, PR scope, delivery workflow, and reviewer respectively.",
    "The git-tag (pr23-start/pr23-complete) and five-section completion-report conventions are carried forward from prompt_pr5.txt for pipeline consistency; the template does not forbid them.",
    "The new streaming test file is named tests/test_router_stream.py; the plan's PR 23 section lists no suggested files, so naming follows the existing test-file convention.",
    "RouterStream, StreamFailurePolicy, and StreamRestart live in router.py rather than a new module, matching the plan's recommended package structure which lists no streaming module.",
    "StreamFailurePolicy uses lowercase StrEnum values ('restart', 'raise') matching the codebase's existing StrEnum conventions; StreamRestart is a suggested name - the executing model may rename it if a clearer name fits the codebase, reporting the rename in the completion report.",
    "The recognized-shape check covers both chat and legacy completions streams via the same choices/finish_reason attributes; no operation-string allowlist exists anywhere.",
    "NormalizedStream is exported from the package root (not only from adapters.base) so custom adapter authors opt in without deep imports; this is the natural reading of the pinned 'documented opt-in'."
  ]
}
