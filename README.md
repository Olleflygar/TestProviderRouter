# providerrouter

Baseline provider router for OpenAI-compatible and Anthropic model calls.

This first version intentionally uses in-memory round-robin routing only. Scoring,
health, and adaptive provider selection are future layers.
